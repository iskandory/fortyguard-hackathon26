# Supabase (anon key + RLS is the intended public-client pattern).
# Leave BOTH blank to run entirely on the bundled cached demo dataset.
# The FortyGuard API key never lives here — it stays server-side with the backend lane.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
